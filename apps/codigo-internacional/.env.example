# Worker base URL the landing page posts leads to (production).
# Leave empty in dev — Vite proxies /api to the local worker (wrangler dev :8787).
VITE_API_BASE=https://content-engine.hudsonargollo2.workers.dev
